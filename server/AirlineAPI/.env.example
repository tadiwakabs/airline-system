# Frontend Connection
FRONTEND_URL=http://localhost:5173

# Database Connection
DB_HOST=mysql.tadzz.net
DB_PORT=61029
DB_NAME=airlineSystem
DB_USER=<user>
DB_PASSWORD=<password>
DB_SSL_MODE=Required

# JWT Authentication (add in a random value for the JWT_KEY)
JWT_KEY=<key>
JWT_ISSUER=AirlineApi
JWT_AUDIENCE=AirlineClient
